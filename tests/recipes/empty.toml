schema_version = 1

# Test recipe used as a template; the test rewrites paths to absolute.

[input]
iso = "ABSOLUTE_PATH_REWRITTEN_BY_TEST"

[output]
iso = "ABSOLUTE_PATH_REWRITTEN_BY_TEST"
manifest = "ABSOLUTE_PATH_REWRITTEN_BY_TEST"
